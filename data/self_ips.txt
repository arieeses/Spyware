# 自有基础设施 IP / CIDR —— 命中即排除(subconverter / 监控 / 节点 / 面板)
# 纪律: 只填你自己的具体 IP / CIDR, 绝不填整个机房 ASN(否则同机房攻击者免检)
# 生产: 从节点清单自动同步, 加/删节点时更新
203.0.113.10/32      # 示例: subconverter 服务器
203.0.113.0/24       # 示例: 你的一段节点
